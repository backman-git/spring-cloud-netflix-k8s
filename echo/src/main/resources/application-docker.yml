spring:
  profiles:
    active: docker
  application:
    name: echo-service

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    instance:
      preferIpAddress: true